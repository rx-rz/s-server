// import { ref } from "firebase/storage";
// import { ctx } from "../ctx";

// const base64ToBlob = async (base64: string, imgNam) => {
//   const binary = atob(base64.split(",")[1]);
//   const imgBlob = await fetch(binary).then((res) => {
//     let blob;
//     blob = res.blob();
//     return blob;
//   });
//   return imgBlob;
// };

// const uploadBlobAndGetImageURL = async (blob: Blob) => {
//     const storageRef = ref(ctx.storage, )
// }
